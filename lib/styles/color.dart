import 'dart:convert';

import 'package:flutter/material.dart';
var horizontalLineColor=Colors.black26.withOpacity(.2);
var whiteColor=Colors.white;
var blackColor=Colors.black;
var accentColor=Color.fromRGBO(208, 146, 68,1.0);
var shadowButtonColor=Color(0xFF6078ea).withOpacity(.3);
var primaryColor=Color.fromRGBO(72, 72, 72,1.0);
var companyClipper= [
  const Color.fromRGBO(50, 50, 50, 1.0),
  const Color.fromRGBO(72, 72, 72, 0.7)
];

//carousel of products
var blackColor100=Color.fromARGB(100, 0, 0, 0);

//container shadow in favorite page
var shadowColor = Color.fromRGBO(173, 179, 191, 0.3);

//transparent color
var transparentColor = Colors.transparent;

//border color in profile page
var borderColor = Color.fromRGBO(246, 140, 31, 1.0);

//list color
var listColor = [
  Color.fromRGBO(0, 0, 0, 0.3),
  Color.fromRGBO(0, 0, 0, 0.5),
];

//white 70
var white70 = Colors.white70;

// black 26
var black26 = Colors.black26;

// black 12var
var black12 = Colors.black12;